pipeline {
    agent any
    enviornment{
        //you can call your credentials here
    }
    stages{
        stage('clone code'){
            steps{
                checkout scm
            }
        }
    }
}